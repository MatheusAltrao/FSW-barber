import { Loader2 } from 'lucide-react';

const Loading = () => {
  return (
    <div>
      <Loader2
        className="animate-spin"
        size={20}
      />
    </div>
  );
};

export default Loading;
